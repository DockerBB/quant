fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pandas>=1.5.0
numpy>=1.23.0
pyyaml>=6.0
baostock>=0.8.8
pyarrow>=10.0.0
apscheduler>=3.10.0
eval_type_backport>=0.3.0
# akshare>=1.16.0   # Requires Python 3.9+, install separately in newer Python env
# duckdb>=0.8.0      # Requires C++ build tools, install separately if needed
